# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(rover_localizationTaskLib)
ADD_LIBRARY(${ROVER_LOCALIZATION_TASKLIB_NAME} SHARED 
    ${ROVER_LOCALIZATION_TASKLIB_SOURCES})
add_dependencies(${ROVER_LOCALIZATION_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${ROVER_LOCALIZATION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ROVER_LOCALIZATION_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ROVER_LOCALIZATION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ROVER_LOCALIZATION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ROVER_LOCALIZATION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ROVER_LOCALIZATION_TASKLIB_HEADERS}
    DESTINATION include/orocos/rover_localization)

